# Running project
We are running with local models, but should be able to swap out with any openai compatible model. Ideally non-thinking. We use Docker for the environment to make it more portable and consistent. But of course you could run your local node environment instead.

## Commands
```bash lint```
```bash test```
```bash run```
```bash eval```
